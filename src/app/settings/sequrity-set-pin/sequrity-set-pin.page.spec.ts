import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SequritySetPinPage } from './sequrity-set-pin.page';

describe('SequritySetPinPage', () => {
  let component: SequritySetPinPage;
  let fixture: ComponentFixture<SequritySetPinPage>;

  // beforeEach(async(() => {
  //   fixture = TestBed.createComponent(SequritySetPinPage);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
